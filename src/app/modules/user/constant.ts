const userSearchableFields: string[] = [
  "name",
  "email",
  "contactNo",
  "address",
];

const userFilterableFields: string[] = ["searchTerm", "id", "email"];

export { userSearchableFields, userFilterableFields };
